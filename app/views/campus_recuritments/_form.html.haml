= form_for @campus_recuritment do |f|
  - if @campus_recuritment.errors.any?
    #error_explanation
      %h2= "#{pluralize(@campus_recuritment.errors.count, "error")} prohibited this campus_recuritment from being saved:"
      %ul
        - @campus_recuritment.errors.full_messages.each do |msg|
          %li= msg

  .field
    = f.label :title
    = f.text_field :title
  .field
    = f.label :position
    = f.text_field :position
  .field
    = f.label :department
    = f.text_field :department
  .field
    = f.label :deadline
    = f.datetime_select :deadline
  .field
    = f.label :description
    = f.text_field :description
  .actions
    = f.submit 'Save'
